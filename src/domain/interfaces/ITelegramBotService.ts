import { TransactionInput } from '../entities/Transaction';
import { Category, BudgetInfo, Account } from '../entities/index';

export class TelegramBotService {
  // Telegram Bot API integration (simplified for reference)
  // In production, use Telegraf or similar framework
}
